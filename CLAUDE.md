- 今いるディレクトリは SOUP Rewards アプリです。
これから、実機での挙動確認とログチェックを手伝ってください。

やってほしいこと：

1. `flutter analyze` を実行して、エラー/警告の件数と内容を要約してください。
   - もし issue が出たら、今回の変更に関係しそうなものだけを優先的に直す提案をしてください。

2. 問題なければ、iPhone 実機に向けて

   flutter run -d 00008140-000055483ABA801C --target=lib/main.dart

   を実行し、ビルドとインストールが終わるまでログを監視してください。

3. アプリがデバイスに入ったあと、僕が次の順番で操作します。
   各ステップの直後にログを読み取って、状態をコメントしてください。

   (A) アプリを起動：
       - デイリーくじのフルスクリーンが自動で表示されるかどうかを画面上で確認します。
       - ログ上では、くじの読み込みやポイント付与の処理にエラーが出ていないかチェックしてください。

   (B) ポイントタブを開いて、「本日のポイントを受け取る」をタップ：
       - ゲストでもくじ画面が開く想定です。
       - 会員登録画面に飛んでしまっていないか、permission-denied などの Firestore エラーが出ていないか確認してください。

   (C) QRスキャンタブを開いて、QRスキャン画面を表示 → すぐ戻るボタンでポイントタブかホームに戻ります：
       - 戻るボタンの挙動に問題がないか（pop で戻れているか）。
       - カメラ起動周りで警告やエラーが出ていないか。
       - 上部テキストのレイアウト関連の例外が出ていないか。

   (D) ポイントタブで「ポイントを使う（給油券）」をタップ（ゲストのまま）：
       - 画面上では「給油券を使うには会員登録が必要です」のようなダイアログが出て、
         ログイン/会員登録画面に誘導される想定です。
       - Firestore の読み書きでエラーが出ていないか確認してください。

4. (A)〜(D) それぞれについて、
   - どんなログが出ていたか（重要な行だけでOK）
   - 問題がある場合は「どのメソッド／ファイルが原因か」と「具体的な修正案」
   を箇条書きで教えてください。

5. 最後に、「この状態で TestFlight / App Store Connect に出してよさそうか」を、
   - OK
   - 条件付きOK（〇〇だけ直したらOK）
   - まだNG（××が致命的）
   のどれかでコメントしてください。

ビルドが極端に遅い or 止まりそうな場合は、その地点で一度ログ解析だけ先にまとめてもらって大丈夫です。